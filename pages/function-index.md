---
title: Function index
---

# Function index

Every public function NAF ships, and the package it comes from. Generated from
the published packages on every build, so it cannot go stale.

| Package | Functions | Chapter |
|---|---|---|
| **`naf/framework`** | `abort()` `app()` `config()` `env()` `event()` `guard()` `json()` `log()` `param()` `plugin()` `redirect()` `refresh()` `request()` `response()` `route()` `simple_render()` `simple_view()` | Core |
| **`naf/auth`**<br><span style="font-size:.85em;opacity:.7">`composer require naf/auth`</span> | `auth()` | [Authentication and permissions](auth.md) |
| **`naf/cli`**<br><span style="font-size:.85em;opacity:.7">`composer require naf/cli`</span> | `command()` | [Console commands](console.md) |
| **`naf/client`**<br><span style="font-size:.85em;opacity:.7">`composer require naf/client`</span> | `client()` | [HTTP client](http-client.md) |
| **`naf/database`**<br><span style="font-size:.85em;opacity:.7">`composer require naf/database`</span> | `database()` | [Database](database.md) |
| **`naf/form`**<br><span style="font-size:.85em;opacity:.7">`composer require naf/form`</span> | `csrf()` `error()` `error_class()` `has_error()` `is_post()` `memory()` `memory_checked()` `memory_selected()` `validator()` | [Forms and validation](forms.md) |
| **`naf/i18n`**<br><span style="font-size:.85em;opacity:.7">`composer require naf/i18n`</span> | `lang()` `t()` `translator()` | [Translations](translations.md) |
| **`naf/mail`**<br><span style="font-size:.85em;opacity:.7">`composer require naf/mail`</span> | `mail()` `mailer()` | [Sending mail](mail.md) |
| **`naf/mcp`**<br><span style="font-size:.85em;opacity:.7">`composer require naf/mcp`</span> | `tokens()` `tool()` | [MCP tools](mcp.md) |
| **`naf/oauth-client`**<br><span style="font-size:.85em;opacity:.7">`composer require naf/oauth-client`</span> | `oauth()` `oauth_button()` `oauth_token()` `render_oauth_view()` | [Signing in with a provider](oauth-client.md) |
| **`naf/oauth-server`**<br><span style="font-size:.85em;opacity:.7">`composer require naf/oauth-server`</span> | `token()` | [Being the provider](oauth-server.md) |
| **`naf/orm`**<br><span style="font-size:.85em;opacity:.7">`composer require naf/orm`</span> | `em()` `repo()` | [ORM and repositories](orm.md) |
| **`naf/queue`**<br><span style="font-size:.85em;opacity:.7">`composer require naf/queue`</span> | `queue()` | [Queues and workers](queues.md) |
| **`naf/schedule`**<br><span style="font-size:.85em;opacity:.7">`composer require naf/schedule`</span> | `scheduler()` | [Scheduled jobs](scheduling.md) |
| **`naf/session`**<br><span style="font-size:.85em;opacity:.7">`composer require naf/session`</span> | `session()` | [Sessions](sessions.md) |
| **`naf/view`**<br><span style="font-size:.85em;opacity:.7">`composer require naf/view`</span> | `asset()` `render()` `s()` `view()` | [Views and templates](views.md) |

*51 functions across 16 packages.*
